# Port (Render sets this automatically)
PORT=3002

# Shared secret for webhook authentication (optional but recommended)
# Set the same value in Mission Control's OPENCLAW_WEBHOOK_SECRET
OPENCLAW_WEBHOOK_SECRET=your_secret_here
